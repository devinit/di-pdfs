{
	"scripts": {
		"bootstrap": "lerna bootstrap --hoist",
		"publish": "lerna publish"
	},
	"devDependencies": {
		"git-pre-hooks": "^1.2.0",
		"lerna": "2.7.0"
	},
	"git-pre-hooks": {
		"pre-push": [
			"lerna run lint && lerna run build"
		]
	}
}
