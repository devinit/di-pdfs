/**
 * base configs
 */
import {red, blue, purple} from '../../../theme/colors';

export const base = {
  colors: [red, purple, blue],
  coloring: 'color',
  labeling: {
    showLabels: true
  },
  linearAxis: {
    showAxis: true,
    showGridlines: true,
  },
  categoryAxis: {
    showAxis: true,
    innerPadding: 0.1,
    outerPadding: 0
  }
};
